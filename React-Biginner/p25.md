# Chapter 25. Creating Records via API

## Introduction
- The Creating Records via API page helps the Sunrise Clinic Scheduling Desk team handle the clinic flow without confusion.
- The React front end calls /api/patients so this screen stays in sync with the backend API.

## Key Concepts
- Fetching data is sliding a folder from the records clerk to the desk so everyone sees the latest notes.
- Creating Records via API is the reason our frontend talks to the API, pulling new arrivals and sending updates without extra clicks.

## Step-by-step Implementation
1. Lay out the Creating Records via API intro card
   - Explanation Start with a friendly section so the Sunrise Clinic desk knows what Creating Records via API is about.
   ```jsx
   function CreatingRecordsViaIntro() {
     return (
       <section className='intro-card'>
         <h2>Creating Records via API at Sunrise Clinic Scheduling Desk</h2>
         <p>Welcome back, front desk! This section explains why Creating Records via API matters today.</p>
       </section>
     );
   }
   ```
   - Codex help: Ask Codex to build the CreatingRecordsViaIntro section with a warm heading and short explanation for Creating Records via API.

2. Display Creating Records via API data
   - Explanation Map over sample records so Creating Records via API looks like live data from the API.
   ```jsx
   const sampleRecords = [
     { id: '1', label: 'Maya' },
     { id: '2', label: 'Dev' }
   ];
   function CreatingRecordsViaList() {
     return (
       <div>
         {sampleRecords.map(record => (
           <article key={record.id}>
             <strong>{record.label}</strong>
             <p>Creating Records via API note</p>
           </article>
         ))}
       </div>
     );
   }
   ```
   - Codex help: Let Codex format those sample records into cards so Creating Records via API looks ready for real data.

3. Call the API for Creating Records via API
   - Explanation Connect to /api/patients with POST so the sunrise clinic frontend keeps Creating Records via API mirrored with the backend.
   ```jsx
   const [records, setRecords] = useState([]);
   useEffect(() => {
     async function syncData() {
       const response = await fetch('/api/patients', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({ name: 'New patient', email: 'frontdesk@clinic.com' }),
       });
       if (!response.ok) throw new Error('Failed to load Creating Records via API');
       const data = await response.json();
       setRecords(data);
     }
     syncData();
   }, []);
   return <p>Creating Records via API records: {records.length}</p>;
   ```
   - Codex help: Ask Codex to craft the fetch block that calls /api/patients for Creating Records via API and stores the result.

## How to Use Codex Here
- Generate a React component that fetches Creating Records via API from /api/patients and lists it in friendly cards.
- Refactor the Creating Records via API component so it splits the header and list parts into child components.
- Add loading and error messages for the Creating Records via API fetch that talks to /api/patients.

## Mini-Exercise (Optional)
- Wire a small filter that lets you call /api/patients with a query so you can see subsets of Creating Records via API.
